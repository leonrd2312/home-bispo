# Gerado por: cloudflared tunnel create homebispo
# Depois de rodar o comando acima, substitua <TUNNEL_ID> abaixo (aparece
# no output do comando e no nome do arquivo .json criado em ~/.cloudflared/).
tunnel: <TUNNEL_ID>
credentials-file: /data/data/com.termux/files/home/.cloudflared/<TUNNEL_ID>.json

ingress:
  - hostname: casa.fbsp.com.br
    service: http://localhost:8000
  - service: http_status:404
